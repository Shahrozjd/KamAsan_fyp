<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kaamasaan">

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ServiceProviderMessageActivity"></activity>
        <activity
            android:name=".SplashScreenActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SendWorkRequestActivity" />
        <activity
            android:name=".WillingnessActivity"
            android:label="Willingness" />
        <activity android:name=".SendVisitRequestActivityForBroadCast" />
        <activity android:name=".SendVisitRequestActivity" />
        <activity android:name=".CustomerMessageActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivityBroadCast"
            android:label="@string/title_activity_maps_broad_cast" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".EditCustomerProfileActivity" />
        <activity
            android:name=".MapsActivitySPEditProfile"
            android:label="@string/title_activity_maps_s_p_edit_profile" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".EditSPProfileActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FoundSPListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FoundSPMapActivity"
            android:label="@string/title_activity_found_s_p_map"
            android:screenOrientation="portrait" />
        <activity android:name=".SearchServiceProviderActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".CustomerHomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".CustomerSignUpActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MapsActivitySPSignup"
            android:label="@string/title_activity_maps_s_p_signup"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ServiceProviderHomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ServiceProviderSignUpPart3Activity"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".ServiceProviderSignUpPart2Activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ServiceProviderSignUpPart1Activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".VerifyPhoneActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>